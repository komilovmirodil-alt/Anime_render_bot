# Workspace Notes

- Project type: Python Telegram bot
- Entry point: `python -m src.main`
- Environment variable: `BOT_TOKEN` in `.env`
- Keep code inside `src/` with modular handlers under `src/bot/handlers/`
